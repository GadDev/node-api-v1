{
  "verbose": true,
  "watch": ["src"],
  "ext": "ts",
  "exec": "concurrently \"npx tsc -p tsconfig.build.json --watch \" \"ts-node src/index.ts\"",
  "ignore": [".git", "node_modules/**/node_modules"],
  "env": {
    "NODE_ENV": "development"
  },
  "delay": 2500
}
